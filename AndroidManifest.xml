<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      android:versionCode="1"
      android:versionName="1.0" package="com.allplayers.android">
    <uses-sdk android:minSdkVersion="8"/>
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application android:icon="@drawable/icon" android:label="@string/app_name" android:debuggable="true">
        <activity android:name=".Login"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- <activity android:name="Login" android:label="@string/app_name" android:theme="@android:style/Theme.NoTitleBar"></activity>
          replaced the FirstScreen page with Login (moved search groups to Login) -->
         <activity android:name="MainScreen" android:label="@string/app_name" android:theme="@android:style/Theme.NoTitleBar"></activity>
        <activity android:name="MessageActivity" android:label="@string/app_name" android:theme="@android:style/Theme.NoTitleBar"></activity>
        <activity android:name="PhotosActivity" android:label="@string/app_name" android:theme="@android:style/Theme.NoTitleBar"></activity>
        <activity android:name="EventsActivity" android:label="@string/app_name" android:theme="@android:style/Theme.NoTitleBar"></activity>
        <activity android:name="AccountActivity" android:label="@string/app_name" android:theme="@android:style/Theme.NoTitleBar"></activity>
		
		<!-- Start Find Groups Activities -->
		<activity android:name="FindGroupsActivity" android:label="@string/app_name" android:theme="@android:style/Theme.NoTitleBar"></activity>
        <activity android:name="SearchGroupsListActivity" android:label="@string/app_name" android:theme="@android:style/Theme.NoTitleBar"></activity>
        <!-- End Find Groups Activities -->
        
		<!-- Start Group Activities -->
		<activity android:name="GroupsActivity" android:label="@string/app_name" android:theme="@android:style/Theme.NoTitleBar"></activity>
		<activity android:name="GroupPageActivity" android:label="@string/app_name" android:theme="@android:style/Theme.NoTitleBar"></activity>
		<activity android:name="GroupMembersActivity" android:label="@string/app_name" android:theme="@android:style/Theme.NoTitleBar"></activity>
		<activity android:name="GroupEventsActivity" android:label="@string/app_name" android:theme="@android:style/Theme.NoTitleBar"></activity>
		<activity android:name="EventsDisplayActivity" android:label="@string/app_name" android:theme="@android:style/Theme.NoTitleBar"></activity>
		<activity android:name="GroupAlbumsActivity" android:label="@string/app_name" android:theme="@android:style/Theme.NoTitleBar"></activity>
		<activity android:name="AlbumPhotosActivity" android:label="@string/app_name" android:theme="@android:style/Theme.NoTitleBar"></activity>
		<activity android:name="PhotoDisplayActivity" android:label="@string/app_name" android:theme="@android:style/Theme.NoTitleBar"></activity>
		<!-- End Group Activities -->
		
		<!-- Start Message Activities -->
		<activity android:name="MessageInbox" android:label="@string/app_name" android:theme="@android:style/Theme.NoTitleBar"></activity>
		<activity android:name="MessageSent" android:label="@string/app_name" android:theme="@android:style/Theme.NoTitleBar"></activity>
		<activity android:name="MessageViewSingle" android:label="@string/app_name" android:theme="@android:style/Theme.NoTitleBar"></activity>
		<activity android:name="MessageReply" android:label="@string/app_name" android:theme="@android:style/Theme.NoTitleBar"></activity>
		<!-- End Message Activities -->
		
		
    </application>
</manifest>